spring.application.name=irecon

# Profile Configuration
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Local Development Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/irecon
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

server.port=8090

# Logging Configuration
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

server.servlet.context-path=/

spring.jackson.property-naming-strategy=SNAKE_CASE
spring.jackson.default-property-inclusion=NON_NULL

#Local Mysql DB
#spring.datasource.url=jdbc:mysql://localhost:3306/shiva_db
#spring.datasource.username=shiva
#spring.datasource.password=pass1234

# Production Database Configuration
#spring.datasource.url=postgresql://${PGUSER}:${POSTGRES_PASSWORD}@${RAILWAY_PRIVATE_DOMAIN}:${RAILWAY_TCP_PROXY_PORT}/${PGDATABASE}
#spring.datasource.username=${PGUSER}
#spring.datasource.password=${POSTGRES_PASSWORD}
#spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=false
#spring.jpa.properties.hibernate.format_sql=false

# Server Configuration
# server.port=${PORT:8090}
# server.servlet.context-path=/

# Logging Configuration
logging.level.root=INFO
logging.level.org.hibernate=ERROR

logging.level.org.springframework=DEBUG
logging.level.com.impacto.irecon=DEBUG

# Jackson Configuration
#spring.jackson.property-naming-strategy=SNAKE_CASE
#spring.jackson.default-property-inclusion=NON_NULL

# Performance Tuning
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=300000
